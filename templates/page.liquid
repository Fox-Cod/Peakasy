<div style="max-width:760px;margin:90px auto;padding:0 28px 80px;">
  <h1 style="font-family:'Cormorant Garamond',serif;font-size:2.6rem;font-weight:700;color:var(--text);margin-bottom:28px;">{{ page.title }}</h1>
  <div style="color:var(--text2);line-height:1.75;font-size:.95rem;">{{ page.content }}</div>
</div>
